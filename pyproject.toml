[project]
name = "lab"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pre-commit>=4.5.1",
    "pytest>=9.0.2",
]

[tool.pytest]
pythonpath = ["src"]
testpaths = ["test"]
